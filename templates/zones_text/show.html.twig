{% extends 'base.html.twig' %}

{% block title %}ZonesText{% endblock %}

{% block body %}
    <h1>ZonesText</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zones_text.id }}</td>
            </tr>
            <tr>
                <th>Texts</th>
                <td>{{ zones_text.texts }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ zones_text.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zones_text_index') }}">back to list</a>

    <a href="{{ path('app_zones_text_edit', {'id': zones_text.id}) }}">edit</a>

    {{ include('zones_text/_delete_form.html.twig') }}
{% endblock %}
