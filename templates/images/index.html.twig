{% extends 'layout.html.twig' %}

{% block title %}{{'Images list'|trans }}{% endblock %}

{% block body %}
<div class="relative-container">
    <div class="container">
        <div class="row">
        <figure class="text-center">
            <blockquote class="blockquote">
                 <h1>{{ 'Images list'|trans }}</h1>
                <a href="{{ path('app_images_new') }}" class="btn btn-success">{{ 'Add'|trans }}</a>
            </blockquote>
        </figure>
            <table class="table table-striped table-dark">
                <thead class="text-center">
                    <tr>
                        <th>Id</th>
                        <td>{{ 'Game'|trans }}</td>
                        <th>{{ 'Name'|trans }}</th>
                        <th>{{ 'Link'|trans }}</th>
                        <th>{{ 'Title'|trans }}</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody class="text-center">
                {% for image in images %}
                    <tr>
                        <td>{{ image.id }}</td>
                        <td>{{ image.game.name }}</td>
                        <td>{{ image.name }}</td>
                        {# <td>{{ image.link }}</td> #}
                        <td>
                            <img src="{{ asset(image.link) }}" alt="{{ image.name }}" style="max-width: 100px; height: auto;">
                            {{ image.link }}
                        </td>
                        <td>{{ image.title ? 'Yes' : 'No' }}</td>
                        <td>
                            <a href="{{ path('app_images_edit', {'id': image.id}) }}" class="btn btn-warning">{{ 'Edit'|trans }}</a>
                            <a href="{{ path('app_images_delete', {'id': image.id}) }}" class="btn btn-danger">{{ 'Delete'|trans }}</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
