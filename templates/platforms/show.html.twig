{% extends 'base.html.twig' %}

{% block title %}Platforms{% endblock %}

{% block body %}
    <h1>Platforms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ platform.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_platforms_index') }}">back to list</a>

    <a href="{{ path('app_platforms_edit', {'id': platform.id}) }}">edit</a>

    {{ include('platforms/_delete_form.html.twig') }}
{% endblock %}
