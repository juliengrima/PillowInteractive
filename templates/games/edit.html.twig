{% extends 'layout.html.twig' %}

{% block title %}{{'Edit Games'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Games'|trans}}</h1>
    <div class="relative-container">
        <div class="container">
            <div class="row">
            <figure class="text-center">
                <blockquote class="blockquote">
                    <h1>{{ 'Edit Games'|trans }}</h1>
                    <a href="{{ path('app_games_index') }}" class="btn btn-warning">{{ 'back to list'|trans }}</a>
                </blockquote>
            </figure>
            {{ include('games/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('games/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
